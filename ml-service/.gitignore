# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/

# Editor / IDE
.vscode/
.idea/
*.swp

# Testing / coverage
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ML artifacts (trained models stay out of git; Railway pulls them at start
# or operator commits them after deciding the size is acceptable).
trained_models/*
!trained_models/.gitkeep
*.pkl
*.joblib

# Raw training data — large CSV archive, regenerated by
# `scripts/download_football_data.py`.
data/raw/
data/processed/

# Env
.env
.env.local
